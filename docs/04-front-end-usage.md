# Step 4: Using the Front-End

(Optional) If your workflow includes a custom front-end:

1. Navigate to the `frontend/` folder
2. Install dependencies:
   ```bash
   npm install
   npm run dev
   ```
3. Configure the `.env` file with correct API endpoints
